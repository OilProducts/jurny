#version 460
// generate_rays.comp — K0 primary ray generation (placeholder)
layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;
void main() {}
// Writes rays to RayQueueIn with camera-relative origin and direction.
